spring.application.name=authify
# Use variables with local defaults (the part after the colon)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/authify_app}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root@123}

server.port=${PORT:8081}
spring.jpa.hibernate.ddl-auto=update
server.servlet.context-path=/api

# Security & Mail
jwt.secret.key=${JWT_SECRET:yourverylongandsecurerandomsecretkeyhereatleast32chars}

spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:9f1a11001@smtp-brevo.com}
spring.mail.password=${MAIL_PASSWORD:bsk1jG2yoftK3Od}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from =kedargajmal8@gmail.com
